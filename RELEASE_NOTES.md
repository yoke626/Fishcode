# FISHCODE v0.1.4

本版把**会话删除**从独立的「会话管理」窗口改到 **dsh 侧边栏对话条的三点菜单**：删除不再需要离开主界面，随手就能清掉废弃的聊天框。

## 会话删除（三点菜单）

- 在 dsh 侧边栏的任意对话条上点 **⋮**，菜单里新增「**删除会话**」；点击后主进程弹窗二次确认，确认即删除。
- 二次确认**按键安全**：取消是默认按钮，按 ESC 或回车都只取消；只有显式点击「删除」才会真正删除。
- 删除前自动保护：**当前打开的会话**与**正在运行（最近有写入）的会话**拒绝删除；找不到的会话提示可能已删除。
- 删除后主窗口自动刷新，侧边栏同步消失。
- 移除了上一版引入的「会话管理」独立窗口，也一并解决了进入删除流程后卡住的问题。
- 会话 id 直接从页面行读取，由随包分发的独立 Node 运行时定位并删除 `~/.dsh/sessions` 下的会话文件夹，不依赖 dsh 后端提供删除接口。

## 自动更新

- 启动后约 5 秒在后台检查一次新版本；托盘菜单新增「检查更新…」可随时手动检查。
- 发现新版弹窗提示，确认后开始下载，进度通过系统通知展示；下载完成可一键「立即重启安装」。
- 更新源直接走 GitHub Releases 资产直链（不经 api.github.com，境内网络更友好）；网络受限时可一键「手动下载」跳转发布页。
- 覆盖式安装，`~/.dsh` 与 `%APPDATA%/fishcode`（设置、密钥、会话数据）完全保留，无需先卸载。

> **重要**：首个带更新器的版本（0.1.3）需要手动安装一次；此后旧版本即可自动检测新版本。

> 平台说明：macOS 未签名（Gatekeeper 会拦截自动更新）保持手动安装；Linux 仅 AppImage 版支持自动更新。

## 下载

Windows 用户直接下载 `Fishcode Setup 0.1.4.exe` 覆盖安装即可（数据保留）。
