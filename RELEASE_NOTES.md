# FISHCODE v0.1.6

本版内置 **dsh-routing-suite**，把思维模式路由预设和运行时注入器直接带进桌面应用。

## 新增：dsh-routing-suite

- **dsh-super-injector v0.3.3**
  - 运行时注入器，提供 `dev_*` 工具全家桶：注入、热重载、卸载、插件状态、路由自愈等。
- **Router Standard (experimental)**
  - RL 接口还原：首轮一句话 persona + shell/编辑器，快速进入行动。
- **Router Spec (experimental)**
  - 深度思考优先：分类后注入完整 persona 与 prompt sections，长首轮推理链是设计目标。

新会话时在预设选择器里选择对应预设即可启用。

## 下载

Windows 用户直接下载 `Fishcode Setup 0.1.6.exe` 覆盖安装即可（数据保留）。

> 平台说明：macOS 未签名（Gatekeeper 会拦截自动更新）保持手动安装；Linux 仅 AppImage 版支持自动更新。
