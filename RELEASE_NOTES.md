# FISHCODE v0.1.3

本版带来**自动更新**与**会话管理**：以后无需卸载重装即可升级；侧边栏里删不掉的废弃聊天框也能清掉了。

## 自动更新

- 启动后约 5 秒在后台检查一次新版本；托盘菜单新增「检查更新…」可随时手动检查。
- 发现新版弹窗提示，确认后开始下载，进度通过系统通知展示；下载完成可一键「立即重启安装」。
- 更新源直接走 GitHub Releases 资产直链（不经 api.github.com，境内网络更友好）；网络受限时可一键「手动下载」跳转发布页。
- 覆盖式安装，`~/.dsh` 与 `%APPDATA%/fishcode`（设置、密钥、会话数据）完全保留，无需先卸载。

> **重要**：本版是首个带更新器的版本，请**手动下载本安装包安装一次**；此后旧版本即可自动检测新版本。

> 平台说明：macOS 未签名（Gatekeeper 会拦截自动更新）保持手动安装；Linux 仅 AppImage 版支持自动更新。

## 会话管理

- dsh 侧边栏里删不掉的废弃聊天框，托盘菜单新增「会话管理…」窗口解决：按项目分组展示全部本地会话，支持标题搜索、「勾选空会话」一键选中所有无标题会话、批量删除前二次确认。
- 当前打开的会话与最近有写入的会话自动保护不可删；删除后主窗口自动刷新同步。
- 会话日志为 dsh 的多帧 zstd 格式，由随包分发的独立 Node 运行时逐帧解析；损坏的日志会标「已损坏」列出，且可正常删除。

## 修复

- 无。

## 下载

Windows 用户直接下载 `Fishcode Setup 0.1.3.exe` 覆盖安装即可（数据保留）。
